class Knu

  attr_reader :user, :password, :format

  def initialize(user, password, format = 2)
    @user     = user
    @password = password
    @format   = format
  end

  def receitaCPF(cpf)

  end

end
